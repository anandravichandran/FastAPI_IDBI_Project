# Shared web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
# Multipart parsing for PDF uploads (RAG service).
python-multipart>=0.0.9

# Configuration & validation
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Async HTTP client for DeepSeek V3 (OpenAI-compatible API)
httpx>=0.27,<1.0

# --- RAG service (/rag) ----------------------------------------------------
# Production embedding model + vector store. The RAG service degrades
# gracefully to a dependency-light hashing embedder + in-memory cosine store
# in non-production environments when these are absent, but they are required
# for a real deployment.
sentence-transformers>=3.0
chromadb>=0.5
pypdf>=4.0

# --- Market Data service (/market) -----------------------------------------
# OpenBB Platform SDK powering the Market Data service (stocks, funds, ETFs,
# gold, indices, news, ratios, historical prices). Imported lazily; outside
# production the service falls back to a deterministic synthetic provider when
# it is not installed, so it remains optional for local/offline use.
openbb>=4.3

# --- Optional integrations -------------------------------------------------
# Live market data for the Investment Advisor (falls back to a deterministic
# stub provider when not installed / no PAT configured) — satisfied by the
# 'openbb' dependency above.
