# Shared web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0

# Configuration & validation
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Async HTTP client for DeepSeek V3 (OpenAI-compatible API)
httpx>=0.27,<1.0

# --- Optional integrations -------------------------------------------------
# Live market data for the Investment Advisor (falls back to a deterministic
# stub provider when not installed / no PAT configured):
# openbb>=4.3
#
# Production-grade RAG vector store (both apps ship a dependency-light TF-IDF
# retriever; swap in one of these behind IKnowledgeRepository):
# chromadb>=0.5
# sentence-transformers>=3.0
